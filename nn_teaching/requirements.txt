# PyTorch 神经网络教学代码库依赖
# 安装：pip install -r requirements.txt
# GPU 版 PyTorch 请按 https://pytorch.org/get-started/locally/ 选择对应 CUDA 的索引 URL，勿直接覆盖已装好的 torch/torchvision

torch==2.11.0
torchvision>=0.20.0,<0.27.0

numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0
tqdm>=4.65.0

# ONNX：09 脚本导出 .onnx；PyTorch 2.x 若使用 Dynamo 导出会依赖 onnxscript；onnx 为通用 IR/检查工具
# training_utils.export_model_onnx 已默认 dynamo=False，一般可不装 onnxscript 也能导出
onnx>=1.16.0
onnxscript>=0.1.0

# 可选：仅用 PyTorch 推理时可不装；在 ONNX Runtime 中跑导出的模型时使用
# onnxruntime>=1.16.0
